import Recon from '../';

describe('createProgram', () => {
    it('shoud create a new program.', () => {
        const prog = (
            <div>Hello, world!</div>
        );
        console.log(prog);
    });
});
