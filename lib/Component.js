const tags = require('./tags');

module.exports = function(nodeName, props, children) {
    /*if (nodeName === 'command') {
        const component = tags[nodeName];
        const node = component(props, children);
        return node;
    }

    if (nodeName === 'program') {
        const component = tags[nodeName];
        const node = component(props, children);
        return node;
    }*/

    //else {
        console.log(arguments);
    //}

    return null;
}
